# ── Application Settings ──
APP_TITLE="Feasibility Analysis API"
APP_HOST="0.0.0.0"
APP_PORT=8000
ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# ── Database ──
# (Replace with your own PostgreSQL connection string)
POSTGRES_URL="postgresql://user:password@hostname:5432/dbname?sslmode=require"

# ── Redis ──
# Keep false unless you explicitly want Redis-backed cache/rate limiting.
# QDRANT_BACKEND=none/non also forces Redis off.
REDIS_ENABLED=false
# Use rediss:// if your managed Redis provider requires TLS.
REDIS_URL="redis://default:password@hostname:6379/0"
REDIS_REQUIRED=false

# ── OpenAI ──
OPENAI_API_KEY="sk-proj-your-openai-api-key-here"

# ── LangSmith Observability ──
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
LANGSMITH_API_KEY="your-langsmith-api-key"
LANGSMITH_PROJECT="futureX"

# ── Google Search ──
GOOGLE_API_KEY="your-google-api-key"
GOOGLE_CSE_ID="your-custom-search-engine-id"

# ── Reddit (Optional depending on usage) ──
REDDIT_CLIENT_ID="your-reddit-client-id"
REDDIT_CLIENT_SECRET="your-reddit-client-secret"
REDDIT_USER_AGENT="futurex-feaser/1.0"
REDDIT_PRAW_TIMEOUT_SECONDS=8
REDDIT_SKIP_CRAWLER_FALLBACK=false

# ── Scraping Controls ──
CRAWLER_URL_TIMEOUT_SECONDS=20

# ── Memory Controls ──
NOISE_REMOVER_ENABLED=false
NOISE_REMOVER_MODEL=BAAI/bge-small-en-v1.5
FASTEMBED_CACHE_DIR=/data/cache/fastembed
FASTEMBED_FALLBACK_CACHE_DIR=fastembed_cache
# Use `remote` for a self-hosted Qdrant server reachable by URL.
# Use `local` only for embedded/on-disk Qdrant inside the app process.
QDRANT_BACKEND=remote
QDRANT_URL=http://127.0.0.1:6333
QDRANT_API_KEY=
QDRANT_CLOUD_URL=
QDRANT_CLOUD_API_KEY=
QDRANT_PATH=/data/qdrant
QDRANT_FALLBACK_PATH=qdrant_data
QDRANT_IMAGE=qdrant/qdrant:latest
PRELOAD_RAG_ON_STARTUP=false
PRELOAD_NOISE_REMOVER_ON_STARTUP=false
LECTURE_EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
LECTURE_VECTOR_SIZE=384

# ── Qdrant Full-Node Backups to Google Cloud Storage ──
GCS_CONSOLE_URL="https://console.cloud.google.com/storage/browser/your-bucket"
GCS_BUCKET_NAME="your-bucket"
GCS_ACCESS_KEY_ID="your-gcs-hmac-access-key"
GCS_SECRET_ACCESS_KEY="your-gcs-hmac-secret"
GCS_BACKUP_PREFIX="qdrant/full-node"
BACKUP_CRON="0 2 * * *"
RUN_BACKUP_ON_STARTUP=false

# ── Project Reviewer / Industry Readiness Evaluator ──
PROJECT_REVIEWER_STORAGE_BACKEND=local
PROJECT_REVIEWER_STORAGE_PATH=project_reviewer_data
PROJECT_REVIEWER_GCS_BUCKET_NAME=
PROJECT_REVIEWER_GCS_PREFIX=project-reviewer/code
PROJECT_REVIEWER_WORKDIR=project_reviewer_workdir
PROJECT_REVIEWER_MAX_FILES=350
PROJECT_REVIEWER_MAX_FILE_BYTES=250000
PROJECT_REVIEWER_MAX_CONTEXT_CHARS=80000
PROJECT_REVIEWER_GIT_TIMEOUT_SECONDS=45
PROJECT_REVIEWER_RUNTIME_SANDBOX_ENABLED=false
