[project]
name = "futurex-feaser"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.13.0",
    "langsmith>=0.8.0",
    "python-jose[cryptography]>=3.5.0",
]
